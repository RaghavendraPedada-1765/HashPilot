#!/usr/bin/env bash
# HashPilot Desktop — macOS / Linux Build Script (placeholder)
#
# macOS and Linux support is planned for a future release.
# For now, use scripts/build-desktop.ps1 on Windows.
#
# When macOS is supported:
#   - PyInstaller will produce a .app bundle
#   - electron-builder will produce a .dmg (macOS) or .AppImage (Linux)
#   - Apple Developer account required for Mac App Store distribution
#   - Code signing via APPLE_ID / APPLE_ID_PASSWORD env vars

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo " HashPilot Desktop — macOS / Linux build"
echo " 🚧  Coming soon! Use build-desktop.ps1 on Windows."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
exit 0
